## GitHub Source Fetching

When fetching code, issues, pull requests, repository files, or commit history from GitHub, prefer GitHub's MCP tools over web search or raw browser fetches.

Use web search for GitHub only when the GitHub MCP tools are unavailable, unauthenticated, or missing the needed capability. If falling back, say so briefly.
